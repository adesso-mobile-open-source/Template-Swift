name: PR Label
on:
  pull_request:
    types: [opened, synchronize, reopened, labeled, unlabeled]
permissions:
  contents: read
jobs:
  label:
    uses: adesso-mobile-open-source/workflows-swift/.github/workflows/pr-label-check.yml@main
